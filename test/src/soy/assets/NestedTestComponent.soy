{namespace Templates.NestedTestComponent}

/**
 */
{template .content}
	{delcall NestedTestComponent.components data="all" /}
{/template}

/**
 * @param count
 * @param foo
 * @param id
 */
{template .components}
	{for $index in range($count)}
		{delcall CustomTestComponent}
			{param headerContent: $foo /}
			{param id: $id + 'MyChild' + $index /}
		{/delcall}
	{/for}
{/template}
